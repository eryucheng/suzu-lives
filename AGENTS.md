# Suzu Lives 开发规则

## 动手前

先给出简短计划，再执行。计划至少说明：本次改动的边界、要复用的现有代码或接口，以及验证方式。

- 先查已有实现、接口、组件和项目规则；不靠猜测新造接口。
- 需求存在会明显改变结果的歧义时再确认；其余情况做明确、可回退的合理假设并说明。
- 用户只要求一个局部修正时，改动只覆盖该局部；不顺手重做布局、交互、视觉语言或相邻功能。

## 最小正确改动阶梯

先读完整需求并追踪实际调用链，再在下面第一条可行处停止：

1. 这个东西真的需要新增吗？不需要就不做。
2. 项目里已有相同能力、组件或模式吗？优先复用。
3. 语言标准库能完成吗？优先使用。
4. 平台或浏览器原生能力能完成吗？优先使用。
5. 已安装的依赖能完成吗？优先使用。
6. 仅在以上都不合适时，写满足需求的最小实现。

不要为了“少代码”牺牲边界校验、数据安全、错误处理、可访问性或用户明确要求的行为。

## 修改原则

- Bug 优先找共同根因，而不是逐个路径贴补丁；但不要借修 Bug 无关扩展重构范围。
- 不新增未经请求的抽象、依赖、脚手架、占位 UI 或配置项。
- 能删的不加；选择朴素、可维护、文件数最少的正确方案。
- 新依赖、第三方插件、自动化钩子、打包、提交或推送，都需要用户明确要求。
- 视觉修改必须保留原有设计语言。局部样式调整时，不同时改变遮罩、阴影、边框、排版或布局，除非用户明确要求。

## 验证与交付

- 非平凡逻辑要留下最小有效验证：现有测试、针对性测试或可复现检查。
- UI 改动要在真实组件和真实状态下检查；不能只凭手工拼出的模拟 DOM 判断视觉效果。
- UI 验证默认使用无头浏览器、截图和自动化检查，不主动弹出、置顶或抢占用户正在使用的窗口。
- 只有确实需要检查操作系统原生窗口层时，才可以启动可见窗口，并且必须先征得用户同意；其余窗口行为优先通过主进程或 IPC 测试验证。
- 验证失败、证据不足或有取舍时，直接说明，不假装完成。
- 未经用户要求，不打包、不提交、不推送。
