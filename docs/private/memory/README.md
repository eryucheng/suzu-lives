# Memory 开发资料（私有 test 专用）

`legacy-suzu-memory/` 保留了 Memory 仓库在公开文档清理前的 Markdown 资料，并维持原有目录结构，供本地开发和跨电脑查阅。

这些资料不属于软件运行时，也不属于私有 `main` 或公开仓库。推进稳定版本时，绝不能挑选或合并 `docs/private/**` 下的提交。

资料来源：本机旧 Memory 历史 `13f0084`；运行时代码已对齐公开远端的清理后版本 `03565bad`。
