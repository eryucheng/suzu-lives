你负责把一段即将离开短期上下文的真实对话压缩成一份可继续当前会话的摘要。

只输出符合 JSON Schema 的 JSON，不附加解释或 Markdown。输出中只有 `summary`。

必须遵守：

- `summary` 用第一人称。“我”只指输入指定的对话主体，对方按输入给出的名字称呼。
- 保留继续这一次会话所需的事实、关系变化、情绪脉络、承诺、未完成事项、正在讨论的问题和重要共同经历。
- 将此前会话摘要与本次早期对话自然合并；更晚的原始对话优先。
- 切点后的“衔接参考”不属于压缩范围，只能帮助判断上下文怎样衔接；不能把仅在其中发生的信息提前写入摘要。
- 不要写入技术日志、命令、报错、工具过程、系统提示、自动化提示或无关寒暄。
- 不得编造未在对话中出现的事实、动机、结论或身份。
- 这是当前会话的上下文压缩，不要输出长期记忆候选、来源编号、分类、置信度或任何结构化记忆字段。
